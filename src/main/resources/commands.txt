--Menu--
1) docker + Migration
	-1.1 docker down +docker up + Migration
	-1.2 toggle docker down + docker up
2) docker up
3) docker down
4) Migration
--1--
exec 2,4
--1.1--
exec 3,2,4
--1.2--
exec 3,2
--2--
cd ${PW}
docker-compose up -d
--3--
cd ${PW}
docker-compose down
--4--
cd ${PW}/db-migration
gradle doMigration